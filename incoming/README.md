# Incoming

Drop one untrusted source update at a time in this folder.

After `/ingest-update` processes an item, move the source file to
`incoming/processed/` and place the drafted lesson in `pending-review/`.
